<!doctype html>
<html lang="<%= lang %>" dir="<%= langDir %>">
<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
<%
  var _tagline = t('seo.default_tagline');
  var _title = (typeof title !== 'undefined' && title)
    ? title + ' — ' + SITE_NAME
    : SITE_NAME + ' — ' + _tagline;
  var _kanjiCount = (typeof meta !== 'undefined' && meta && meta.kanji_count) || 2136;
  var _desc = (typeof description !== 'undefined' && description)
    ? description
    : t('seo.default_description', { count: Number(_kanjiCount).toLocaleString('en-US') });
%>
  <title><%= _title %></title>
  <meta name="description" content="<%= _desc %>">
  <% if (typeof noindex !== 'undefined' && noindex) { %><meta name="robots" content="noindex, follow"><% } %>
  <% if (typeof canonical !== 'undefined') { %><link rel="canonical" href="<%= canonical %>"><% } %>
  <% (altLangs || []).forEach(function (a) { %>
  <link rel="alternate" hreflang="<%= a.hreflang %>" href="<%= a.href %>">
  <% }) %>

  <meta property="og:type" content="website">
  <meta property="og:site_name" content="<%= SITE_NAME %>">
  <meta property="og:title" content="<%= _title %>">
  <meta property="og:description" content="<%= _desc %>">
  <meta property="og:locale" content="<%= lang %>">
  <% if (typeof canonical !== 'undefined') { %><meta property="og:url" content="<%= canonical %>"><% } %>
  <meta name="twitter:card" content="summary">

  <link rel="manifest" href="/manifest.webmanifest">
  <meta name="theme-color" content="#fbf9f5" media="(prefers-color-scheme: light)">
  <meta name="theme-color" content="#17150f" media="(prefers-color-scheme: dark)">
  <meta name="mobile-web-app-capable" content="yes">
  <meta name="apple-mobile-web-app-capable" content="yes">
  <meta name="apple-mobile-web-app-status-bar-style" content="default">
  <meta name="apple-mobile-web-app-title" content="<%= SITE_NAME %>">
  <link rel="apple-touch-icon" href="/icons/apple-touch-icon.png">
  <link rel="icon" type="image/png" sizes="192x192" href="/icons/icon-192.png">
  <link rel="icon" href="data:image/svg+xml,<svg xmlns='http://www.w3.org/2000/svg' viewBox='0 0 100 100'><text y='.9em' font-size='90'>字</text></svg>">

  <script>
    // Apply a saved theme before first paint (no flash).
    (function () {
      try {
        var t = localStorage.getItem("theme");
        if (t === "dark" || t === "light") document.documentElement.setAttribute("data-theme", t);
      } catch (e) {}
    })();
  </script>
  <link rel="stylesheet" href="/styles.css">

  <script async src="https://pagead2.googlesyndication.com/pagead/js/adsbygoogle.js?client=ca-pub-3527890793982264"
       crossorigin="anonymous"></script>
</head>
<body class="<%= typeof pageClass !== 'undefined' ? pageClass : '' %>">
<header class="site-header">
  <a class="brand" href="<%= lp %>/">字 <span><%= SITE_NAME %></span></a>
  <nav class="site-nav">
    <a href="<%= lp %>/browse"><%= t('nav.browse') %></a>
    <a href="<%= lp %>/radicals"><%= t('nav.radicals') %></a>
  </nav>
  <form class="search" action="<%= lp %>/search" method="get" role="search" autocomplete="off">
    <input type="search" name="q" id="q" placeholder="<%= t('search.placeholder') %>"
           value="<%= typeof search !== 'undefined' ? search.query : '' %>"
           aria-label="<%= t('search.aria') %>">
    <button type="submit"><%= t('search.button') %></button>
  </form>
  <div class="header-tools">
    <label class="lang-switch">
      <span class="visually-hidden"><%= t('lang.label') %></span>
      <select aria-label="<%= t('lang.label') %>" data-lang-switch>
        <% (langOptions || []).forEach(function (o) { %>
          <option value="<%= o.code %>" <%= o.current ? 'selected' : '' %>><%= o.name %></option>
        <% }) %>
      </select>
    </label>
    <button type="button" class="theme-toggle" aria-label="<%= t('theme.toggle_aria') %>">
      <svg class="ti ti-auto" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.8" aria-hidden="true">
        <circle cx="12" cy="12" r="9"/><path d="M12 3a9 9 0 0 1 0 18z" fill="currentColor"/>
      </svg>
      <svg class="ti ti-light" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.8" stroke-linecap="round" aria-hidden="true">
        <circle cx="12" cy="12" r="4"/>
        <path d="M12 2v2M12 20v2M4.9 4.9l1.4 1.4M17.7 17.7l1.4 1.4M2 12h2M20 12h2M4.9 19.1l1.4-1.4M17.7 6.3l1.4-1.4"/>
      </svg>
      <svg class="ti ti-dark" viewBox="0 0 24 24" fill="currentColor" aria-hidden="true">
        <path d="M21 12.8A9 9 0 1 1 11.2 3a7 7 0 0 0 9.8 9.8z"/>
      </svg>
    </button>
  </div>
</header>
  <main class="wrap">
